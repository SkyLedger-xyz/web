export enum ACTIVITY_VERBS {
  JOINED = 'joined',
  CREATE = 'create',
  REMIND = 'remind',
}

export enum ACTIVITY_NAMES {
  USER = 'user',
  SYSTEM = 'system',
  PROJECT = 'project',
  RELEASE = 'release',
  REPLY = 'reply',
  COMMENT = 'comment',
  TESTER = 'tester',
}
